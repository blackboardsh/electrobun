{
	"name": "electrobun-odin-tree-wgpu",
	"version": "1.0.0",
	"type": "module",
	"description": "An Electrobun Tree Studio with an Odin procedural tree generator rendered on a native WGPU surface",
	"scripts": {
		"start": "hutch electrobun dev",
		"dev": "hutch electrobun dev --watch",
		"watch": "hutch electrobun dev --watch",
		"build": "hutch electrobun build --env=production"
	},
	"dependencies": {
		"electrobun": "file:../../package"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.7.2"
	}
}
