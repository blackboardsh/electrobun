{
	"name": "electrobun-svelte",
	"version": "1.0.0",
	"type": "module",
	"description": "Electrobun app with Svelte and Vite HMR",
	"scripts": {
		"start": "vite build && hutch electrobun dev",
		"dev": "vite build && hutch electrobun dev --watch",
		"dev:hmr": "concurrently \"hutch run hmr\" \"hutch run start\"",
		"hmr": "vite --port 5173",
		"build": "vite build && hutch electrobun build --env=production",
		"build:canary": "vite build && hutch electrobun build --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../../package"
	},
	"devDependencies": {
		"@sveltejs/vite-plugin-svelte": "^5.0.1",
		"@types/bun": "latest",
		"concurrently": "^9.1.0",
		"svelte": "^5.14.1",
		"typescript": "^5.7.2",
		"vite": "^6.0.3"
	}
}
