{
	"name": "electrobun-vanilla-vite",
	"version": "1.0.0",
	"type": "module",
	"description": "Electrobun app with vanilla TypeScript and Vite HMR",
	"scripts": {
		"start": "vite build && hutch electrobun dev",
		"dev": "vite build && hutch electrobun dev --watch",
		"dev:hmr": "concurrently \"hutch run hmr\" \"hutch run start\"",
		"hmr": "vite --port 5173",
		"build:canary": "vite build && hutch electrobun build --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../../package"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"concurrently": "^9.1.0",
		"typescript": "^5.7.2",
		"vite": "^6.0.3"
	}
}
