{
	"name": "electrobun-ui-wgpu",
	"version": "1.0.0",
	"type": "module",
	"description": "Cottontail UI prototype: a reactive, Solid-inspired UI runtime rendered by Dawn \u2014 no webview, no compile step",
	"scripts": {
		"start": "hutch electrobun dev",
		"dev": "hutch electrobun dev --watch",
		"build": "hutch electrobun build --env=production",
		"build:canary": "hutch electrobun build --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../../package"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.7.2"
	}
}
