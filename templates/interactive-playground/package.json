{
  "name": "electrobun-interactive-playground",
  "version": "0.0.1",
  "description": "Interactive playground for exploring Electrobun features",
  "type": "module",
  "scripts": {
    "build": "electrobun build",
    "dev": "electrobun dev",
    "start": "bun run build && bun run dev",
    "build:canary": "electrobun build --canary",
    "start:canary": "bun run build:canary && electrobun dev --canary"
  },
  "dependencies": {
    "electrobun": "latest"
  }
}