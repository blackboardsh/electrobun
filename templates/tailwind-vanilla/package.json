{
	"name": "electrobun-tailwind-vanilla",
	"version": "1.0.0",
	"type": "module",
	"description": "Electrobun app with Tailwind CSS and vanilla TypeScript",
	"scripts": {
		"start": "vite build && dash electrobun dev",
		"dev": "vite build && dash electrobun dev --watch",
		"dev:hmr": "concurrently \"dash run hmr\" \"dash run start\"",
		"hmr": "vite --port 5173",
		"build:canary": "vite build && dash electrobun build --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../../package"
	},
	"devDependencies": {
		"@tailwindcss/vite": "^4.0.0",
		"@types/bun": "latest",
		"concurrently": "^9.1.0",
		"tailwindcss": "^4.0.0",
		"typescript": "^5.7.2",
		"vite": "^6.0.3"
	}
}
