{
	"name": "electrobun-photo-booth",
	"version": "1.0.0",
	"description": "A photo booth app demonstrating getUserMedia with Electrobun",
	"scripts": {
		"start": "dash electrobun dev",
		"dev": "dash electrobun dev --watch",
		"build:canary": "dash electrobun build --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../../package"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
