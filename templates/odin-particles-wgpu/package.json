{
	"name": "electrobun-odin-particles-wgpu",
	"version": "1.0.0",
	"type": "module",
	"description": "An Electrobun app with an Odin main process driving a #soa data-oriented particle simulation on a native WGPU surface",
	"scripts": {
		"start": "electrobun dev",
		"dev": "electrobun dev",
		"watch": "electrobun dev --watch",
		"build": "electrobun build"
	},
	"dependencies": {
		"electrobun": "../../package"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5.7.2"
	}
}
