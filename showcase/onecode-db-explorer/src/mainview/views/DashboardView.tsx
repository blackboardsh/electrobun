export default function DashboardView() {
  return (
    <div class="view">
      <div class="view-card">
        <div class="view-title">Dashboard</div>
        <div class="view-text">
          Connect to a database, open a table from the sidebar, or switch to the SQL tab to run queries.
        </div>
      </div>
    </div>
  );
}
