{
	"scripts": {
		"start": "electrobun run",
		"dev": "electrobun dev",
		"check:zig-mirrors": "../package/bin/dash scripts/check-zig-test-mirrors.ts",
		"check:odin-mirrors": "../package/bin/dash scripts/check-odin-test-mirrors.ts",
		"build:canary": "cd ../package && ./vendors/dash-cli/dash build:release && cd ../kitchen && ../package/bin/dash electrobun build --env=canary",
		"build:stable": "cd ../package && ./vendors/dash-cli/dash build:release && cd ../kitchen && ../package/bin/dash electrobun build --env=stable",
		"start:canary": "electrobun dev --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../package"
	},
	"devDependencies": {
		"@types/bun": "^1.3.8",
		"typescript": "^5.9.3"
	}
}
