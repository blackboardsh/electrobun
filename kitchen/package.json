{
	"scripts": {
		"start": "hutch electrobun run",
		"dev": "hutch electrobun dev",
		"check:zig-mirrors": "hutch scripts/check-zig-test-mirrors.ts",
		"check:odin-mirrors": "hutch scripts/check-odin-test-mirrors.ts",
		"build:canary": "cd ../package && hutch build:release && cd ../kitchen && hutch electrobun build --env=canary",
		"build:production": "cd ../package && hutch build:release && cd ../kitchen && hutch electrobun build --env=production",
		"start:canary": "hutch electrobun dev --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../package"
	},
	"devDependencies": {
		"@types/bun": "^1.3.8",
		"typescript": "^5.9.3"
	}
}
