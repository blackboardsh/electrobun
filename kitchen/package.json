{
	"scripts": {
		"start": "electrobun run",
		"dev": "electrobun dev",
		"check:zig-mirrors": "bun ./scripts/check-zig-test-mirrors.ts",
		"build:canary": "cd ../package && bun build:release && cd ../kitchen && electrobun build --env=canary",
		"build:stable": "cd ../package && bun build:release && cd ../kitchen && electrobun build --env=stable",
		"start:canary": "electrobun dev --env=canary"
	},
	"dependencies": {
		"electrobun": "file:../package"
	},
	"devDependencies": {
		"typescript": "^5.9.3"
	}
}
